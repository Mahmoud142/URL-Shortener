<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortner</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>

<body>
    <div class="container">
        <h1 class="text-center">URL Shortner</h1>
        <br>
        <h4 class="text-center"> Paste the URL to be shortened</h3>
            <form action="/shortUrls" method="POST" class="form-inline">
                <div class="input-group">
                    <input required type="url" id="longUrl" name="longUrl" placeholder="Enter the link here"
                        class="form-control">
                    <button type="submit" class="btn btn-primary btn" class="">Shorten URL</button>
                </div>
            </form>
            <br>
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>Long URL</th>
                        <th>Short URL</th>
                        <th>Clicks</th>
                    </tr>
                </thead>
                <tbody>
                    <% shortUrls.forEach(shortUrl=> { %>
                        <tr>
                            <td><a href="<%= shortUrl.longUrl %>">
                                    <%= shortUrl.longUrl %>
                                </a></td>
                            <td><a href="<%= shortUrl.shortUrl %>">
                                    <%= shortUrl.shortUrl %>
                                </a></td>
                            <td>
                                <%= shortUrl.clicks %>
                            </td>
                        </tr>
                        <% }) %>
                </tbody>
            </table>
    </div>
</body>

</html>